# Prompt 沉淀

本项目由以下提示词驱动开发（参考项目：https://github.com/cooljser/md2wxarticle）。

## 原始提示词

> 一款专为公众号创作者打造的本地神器：告别排版噩梦，左写 Markdown，右看公众号效果，一键复制粘贴！
>
> **痛点解决**：每次写完文章，最烦的就是调格式。Markdown 转成公众号代码不难，但转出来"能用"和"好看"之间，隔着一整个下午的微调——字号、行距、颜色、编号、引用卡片、代码块缩进……每篇文章都要重新来一遍，费时费力，效果还全凭运气。
>
> **核心能力**：
>
> 1. 左写右看：左侧 Markdown 编辑（带工具栏、快捷键），右侧 375px 手机宽度实时预览，所见即所得。
> 2. 主题系统：内置多套主题（如小浣熊紫、极客绿），更支持从公众号文章 zip 包自动提取排版样式生成主题，完整复用原文样式指纹（包括渐变、字号、字距、边框、阴影等），而非仅提取颜色套模板，实现高还原度。
> 3. 一键复制：生成带内联样式的公众号 HTML，可直接粘贴到公众号后台编辑器，样式完整保留，格式不丢。
> 4. 单文件零依赖：纯前端实现，无后端、无构建步骤、无网络请求，双击即用。
>
> GitHub 地址：https://github.com/cooljser/md2wxarticle

## 实现映射

| 提示词能力 | 实现位置 |
| --- | --- |
| 左写右看 + 工具栏 + 快捷键 | `src/routes/index.tsx` |
| Markdown → 内联样式 HTML（含 `:::card/tip/warn/divider` 扩展语法） | `src/lib/md2wx.ts` |
| 主题系统（内置主题、自定义表单、localStorage 持久化） | `src/lib/theme.ts` |
| zip 样式指纹提取 | `src/lib/extract-theme.ts` |
| 一键复制（`text/html` 剪贴板） | `src/routes/index.tsx` 的 `copyHtml` |

## 关键约定

- 所有排版样式以**内联 style 字符串**形式输出，公众号编辑器才能保留。
- 主题存储 key：`mp_tool_custom_themes_v1`；草稿 key：`mp_tool_draft_v1`。
- zip 提取顺序：定位 `#js_content` → 找标题/正文/引用/代码块元素 → 直接复用其完整 `style` 作为样式指纹 → 缺失部分用主色/字号生成的基线样式补齐。
- 全部逻辑在浏览器内完成，无后端、无网络请求（zip 仅本地内存解析）。
